#ifndef VETOR_H
#define VETOR_H

typedef struct{
    int *dados;
    int tamanho_alocado;
    int tamanho_ocupado;
}vetor;

void append(vetor v, int valor);
void show(vetor v);
vetor slice(vetor v, int x0, int x1);
void pop(vetor v);

#endif
