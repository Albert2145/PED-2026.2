#include <stdio.h>
int main(){
    //Questao 3.7
    int soma,numLidos, total, i;

    while(i >= 0){
        printf("Digite um número inteiro. (Digite -1 para parar)\n");
        scanf("%d", &i);
        soma = soma + i;
        numLidos += 1;
    }
    total

    return 0;
}
