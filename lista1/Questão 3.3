#include <stdio.h>
int main()
{
    //Questao 3.3
    int num, prod, i;

    printf("Digite um número inteiro.\n");
    scanf("%d", &num);

    for(i = 1; i <= 10; i++){
        prod = num * i;
        printf("%d * %d = %d\n", num, i, prod);
    }
    
    return 0;
}
