#include <stdio.h>
int main()
{
    //Questao 1.9
    int valor;
    int n100 = 0;
    int n50 = 0;
    int n20 = 0;
    int n10 = 0;
    int n5 = 0;
    int n2 = 0;
    int n1 = 0;

    printf("Informe um valor inteiro de reais.\n");
    scanf("%d", &valor);

    n100 = valor / 100;
    if (valor % 100 > 0){
        n50 = (valor % 100) / 50;
        n20 = ((valor % 100) % 50) / 20;
        n10 = (((valor % 100) % 50) % 20) / 10;
        n5 = ((((valor % 100) % 50) % 20) % 10) / 5;
        n2 = (((((valor % 100) % 50) % 20) % 10) % 5) / 2;
        n1 = ((((((valor % 100) % 50) % 20) % 10) % 5) % 2) / 1;
    }

    printf("%d, %d, %d, %d, %d, %d, %d", n100, n50, n20, n10, n5, n2, n1);

    return 0;
}
