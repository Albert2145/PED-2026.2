#include <stdio.h>
int main()
{
    //Questao 3.1
    int num, i;

    printf("Digite um número inteiro.\n");
    scanf("%d", &num);

    for(i = 1; i <= num; i++){
        printf("%d\n", i);
    }
    
    return 0;
}
